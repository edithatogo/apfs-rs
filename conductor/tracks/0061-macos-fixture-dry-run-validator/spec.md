# macOS APFS fixture dry-run validator

Capability: `M-061`

## Goal

Maintain the local handoff loop by adding macos apfs fixture dry-run validator.

## Safety

No APFS media mutation, raw physical-device access, live mount, encryption bypass, repair, or format.
