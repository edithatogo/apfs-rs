# WinFsp read-only callback contract matrix

Capability: `M-062`

## Goal

Maintain the local handoff loop by adding winfsp read-only callback contract matrix.

## Safety

No APFS media mutation, raw physical-device access, live mount, encryption bypass, repair, or format.
