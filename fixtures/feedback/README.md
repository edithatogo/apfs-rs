# Feedback fixtures

This directory contains synthetic feedback JSON artifacts for testing the task-promotion pipeline before a real macOS APFS fixture is available.

These files are not APFS images and contain no personal data or secrets.
