# Current-Environment Remaining Work

Generated by `tools/current_env_remaining.py`.

- Production remaining total: **17**
- Production items completable in this environment: **0**
- Optional current-environment improvements remaining: **0**

## MVP blockers

| ID | Title | Classification |
|---|---|---|
| `MVP-R001` | Compile, lint, test, and fix the current workspace on Rust-enabled machines | `local_or_platform_required` |
| `MVP-R002` | Generate first real macOS APFS image and manifest | `local_or_platform_required` |
| `MVP-R003` | Run real-fixture feedback loop and convert mismatches into tasks | `local_or_platform_required` |
| `MVP-R004` | Correct APFS parser offsets/semantics against real APFS data | `local_or_platform_required` |
| `MVP-R005` | Full checkpoint ring reconstruction | `local_or_platform_required` |
| `MVP-R006` | Production APFS object-map B-tree traversal | `local_or_platform_required` |
| `MVP-R007` | Production APFS filesystem tree record decoding and metadata/stat mapping | `local_or_platform_required` |
| `MVP-R008` | Production file extent resolution and extraction | `local_or_platform_required` |
| `MVP-R009` | Windows WinFsp read-only mount adapter, smoke tests, and packaging baseline | `local_or_platform_required` |

## Optional current-environment improvements

No required current-environment items remain after this handoff-hardening pass. Open-ended polish examples remain possible but are no longer counted as implementation blockers:

- Add still more synthetic negative fixtures.
- Add more narrative examples to handoff docs.
- Add more static heuristics for possible Cargo failures.

## Conclusion

No production APFS or Windows MVP blockers can be honestly completed in this environment without Rust/macOS/Windows execution. The remaining work is local/platform-dependent; further sandbox work would be optional polish rather than required implementation.
