{
  "name": "apfs-rs-docs-site",
  "version": "0.29.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "audit:docs": "node --version && npm --version && astro --version || true"
  },
  "devDependencies": {
    "astro": "7.0.2",
    "typescript": "^5.9.0",
    "@astrojs/check": "^0.9.9",
    "@astrojs/starlight": "latest"
  },
  "overrides": {
    "yaml": "^2.8.3"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
