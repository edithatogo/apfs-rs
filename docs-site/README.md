# APFS-RS Documentation Site

This is an Astro 7 + Starlight documentation scaffold for APFS-RS.

```bash
cd docs-site
npm install
npm run build
```

The docs site is optional for the CLI/library build but should be part of release readiness.
