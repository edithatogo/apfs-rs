name: release-automation

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  release-plan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - name: Release automation placeholder
        run: |
          echo "cargo-dist and release-plz are configured as scaffolds."
          echo "Enable publishing only after MVP gates pass."
